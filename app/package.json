{
  "name": "kinnisvaraveeb",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://remots22.github.io/kinnisvaraveeb",
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "copyfiles": "^2.4.1"
  },
  "scripts": {
    "build": "mkdir -p build && copyfiles \"*.html\" \"*.js\" build/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
